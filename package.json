{
  "name": "chess-playback",
  "private": true,
  "scripts": {
    "build": "esbuild src/application.js --bundle --outfile=assets/application.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "run-pty": "^6.0.2"
  }
}
