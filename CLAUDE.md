See [README.md](README.md) for project overview.

- Use `bin/jekyll` instead of `bundle exec jekyll`
- Always wait for the user to test before pushing
- Always update the README changelog (and roadmap if applicable) before committing
- Bump `asset_version` in `_config.yml` when changing any files in `assets/`
